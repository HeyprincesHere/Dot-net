using System;

class Example
{
    static int Add(int x, int y)
    {
        return x + y;
    }

    static void Main()
    {
        
        int result = Add(5, 10);

        
        Console.WriteLine("The result : " + result);
    }
}
